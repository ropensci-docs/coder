# Make clean text with only lowercase alphanumeric characters and "\_"

Make clean text with only lowercase alphanumeric characters and "\_"

## Usage

``` r
clean_text(x_name, x)
```

## Arguments

- x_name:

  Name of object to use as prefix

- x:

  character vector

## Value

character vector of the same length as `x`
