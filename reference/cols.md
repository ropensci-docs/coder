# Return all columns from x with names matching "find"

Return all columns from x with names matching "find"

## Usage

``` r
cols(find, x)
```

## Arguments

- find:

  character vector with names to match

- x:

  matrix
